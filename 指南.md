# 状态面板 · 使用与美化指南

一份简短教程：卡作者如何**定义**面板、如何**美化**面板；玩家如何**使用**面板。
详细技术说明见 `AUTHOR.md`（英文，含可直接喂给 AI 的美化 prompt）。

---

## 一、给卡作者：三步定义面板

1. 打开 TavernHelper 的脚本工具栏，点击 **「SP 面板」** 按钮（没有悬浮球——设置只能从这里打开）。
2. **字段** tab → 新增字段：名称（如「心情」「好感度」）、类型（文本/数字/枚举）、
   描述、示例值；数字类型可设最小/最大值（用于进度条）。
3. 保存。若还没有排版，绿色引导卡会提示你去 **样式** tab 选一种排版方式（见下）。

之后 AI 每次回复都会被自动要求在文中输出一段状态块（无需你手动开关），也可以把
**复制状态块** 按钮生成的 JSON 粘贴进开场白，打开对话即自动预填。

---

## 二、给卡作者：如何美化面板

样式 tab 有两种模式：

| 模式 | 适合 | 说明 |
|---|---|---|
| 简易模式 | 不想碰代码 | 一组滑块/取色器，实时预览；保存时按当前字段重新生成排版 |
| 高级模式 | 想要完全自定义外观 | 一个文本框，粘贴完整 HTML + `<style>`，所见即所得预览 |

### 简易模式旋钮

| 旋钮 | 作用 |
|---|---|
| 主色 | 进度条/圆点/徽章的主色，其余颜色旋钮默认跟随它 |
| 圆角 / 字号 | 卡片圆角、正文字号 |
| 标题色 / 状态栏边框色 | 默认跟随主色；改了之后旁边的 ↺ 可以恢复跟随 |
| 主文字颜色 | 正文文字颜色 |
| 按钮颜色 / 按钮边框色 / 按钮文字颜色 | 重试/编辑按钮的填充、边框、文字 |
| 按钮折叠 | 开启后按钮收进一个 **⋯** 小图标，点开才展开 |
| 字体链接 | 粘贴 Google Fonts 的 css2 链接即可换字体（下拉有常用中文字体预设） |

### 高级模式：可用的令牌（token）

| 令牌 | 会被替换成什么 |
|---|---|
| `{字段名}` | 该字段当前存储的值（必须和字段名一字不差；没有值时显示灰色「—」） |
| `{sp_actions}` | 🔄 重试 / ✏ 编辑 两个按钮 |
| `{sp_badge}` | 来源徽章（见下表） |

`{{字段名}}`（双花括号）会原样显示出来。

### 高级模式：完整样式对照表

一张表，把面板里每个能上色/调样式的目标都列出来了；不用背，照着写 CSS 就行：

| 目标 | 可以设置的 CSS |
|---|---|
| `sp.panel` —— 整个面板，写成 `body`、`:root` 或 `.sp-iframe-root` | `background`、`color`、`font-family`、`font-size`、`padding`、`border-radius`、`border`——**没有默认背景，必须自己写一个** |
| 每个 `{字段}` 令牌所在的行/元素 | `color`、`font-size`、`font-weight`、间距 |
| `.sp-ph` / `.sp-ph-dash` —— 空值占位符 | `color`、`opacity`、`font-style` |
| `.sp-actions-wrap` —— 包裹两个按钮的一行 | `display`、`gap`、`justify-content` |
| `.sp-btn-retry` / `.sp-btn-edit` —— 重试/编辑按钮 | `background`、`border`、`color`、`border-radius`、`padding`、字体 |
| `.sp-btn-retry:hover` / `.sp-btn-edit:hover` | 悬停时的 `background`/`filter` |
| `.sp-btn-retry:disabled` —— 生成中的状态 | `opacity`、`cursor` |
| `.sp-badge` —— 来源徽章 | `background`、`border`、`color`、`border-radius`、`font-size`、`padding` |
| `.sp-badge-error` —— 徽章的失败态 | `background`、`border`、`color` |
| `--sp-accent`（可选变量） | 任意颜色，默认 `#7c9cff` |
| `--sp-radius`（可选变量） | 任意长度，默认 `12px` |
| `--sp-text-size`（可选变量） | 任意长度，默认 `13px` |
| `--sp-text-color`（可选变量） | 任意颜色，默认 `rgba(255,255,255,.92)` |
| `--sp-title-color`（可选变量） | 任意颜色，默认跟随 `--sp-accent` |
| `--sp-border-color`（可选变量） | 任意颜色，默认主色 42% 透明度 |
| `--sp-btn-color` / `--sp-btn-border-color` / `--sp-btn-text-color`（可选变量） | 任意颜色，默认由 `--sp-accent` 派生 |
| `--sp-font`（可选变量） | 任意字体栈，默认系统字体 |

以「可选变量」标注的行只有你自己在 CSS 里写 `var(--变量名)` 才会生效；不写也完全没问题，
直接写死颜色即可。

**最容易踩的坑：只写了按钮/文字颜色，忘了给 `sp.panel`（面板根容器）本身写
`background`。** 结果就是文字直接飘在聊天气泡背景上，深色主题尤其明显看不清。

### 借助 AI 生成整套 CSS

不想手写？把 **复制状态块** 按钮导出的 JSON，配合一句风格描述，喂给任意对话式 AI
（Claude、ChatGPT 等）。`AUTHOR.md` 里有一段可以直接复制使用的英文 prompt，把上面这张
表原样交给 AI，AI 产出的结果可以直接粘贴进高级模式文本框。

---

## 三、给玩家：如何使用别人做好的面板

1. **导入卡后**：如果卡自带状态面板设计，聊天顶部会出现一条同意提示——点
   **启用** 才会显示面板，点 **暂不** 则忽略（以后可在管理 tab 里改主意）。
2. **面板出现的位置**：AI 回复消息的下方。
3. **徽章含义**：

   | 徽章文字 | 含义 |
   |---|---|
   | 来自回复 | AI 在这条回复里自己写出了状态块 |
   | 已生成 | 你点了 🔄 重试，引擎单独请求了一次 |
   | 手动 | 你点了 ✏ 编辑，手动改的值 |
   | 生成失败 | 🔄 重试 请求失败了（旧数值会保留，可以再点一次重试） |
4. **🔄 重试**：让 AI 重新为这条消息生成一次状态值。
5. **✏ 编辑**：弹出一个小窗口，直接手动改每个字段的值。
6. **面板没有出现**：可能是这个角色还没有开启（回到管理 tab 检查），或者作者还没有
   定义任何字段/排版（会看到绿色引导卡而不是面板）。
